// will need a package.json and modules require

// const { client } = require("./pg.client");

require("./pg.test-recom").testRecom1();

// comment out which one you don't want to test

// Split it into a separate file, require and module exports from the other file
